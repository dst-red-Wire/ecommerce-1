---
toolchain_lock_file: "{{ repo_root }}/config/contracts/toolchain-lock.yaml"
toolchain_lock: "{{ lookup('ansible.builtin.file', toolchain_lock_file) | from_json }}"
toolchain_versions: "{{ toolchain_lock.versions }}"
local_bin: "{{ ansible_env.HOME }}/.local/bin"
local_share: "{{ ansible_env.HOME }}/.local/share/ecommerce-1"
local_cache: "{{ ansible_env.HOME }}/.cache/ecommerce-1"
node_version: "{{ toolchain_versions.NODE_VERSION }}"
node_sha256: "{{ toolchain_versions.NODE_SHA256_LINUX_X64 }}"
pnpm_version: "{{ toolchain_versions.PNPM_VERSION }}"
go_version: "{{ toolchain_versions.GO_VERSION }}"
go_sha256: "{{ toolchain_versions.GO_SHA256_LINUX_AMD64 }}"
sqlc_version: "{{ toolchain_versions.SQLC_VERSION }}"
sqlc_sha256: "{{ toolchain_versions.SQLC_SHA256_LINUX_AMD64 }}"
ripgrep_version: "{{ toolchain_versions.RIPGREP_VERSION }}"
ripgrep_sha256: "{{ toolchain_versions.RIPGREP_SHA256_LINUX_AMD64 }}"
fd_version: "{{ toolchain_versions.FD_VERSION }}"
fd_sha256: "{{ toolchain_versions.FD_SHA256_LINUX_AMD64 }}"
yq_version: "{{ toolchain_versions.YQ_VERSION }}"
yq_sha256: "{{ toolchain_versions.YQ_SHA256_LINUX_AMD64 }}"
ast_grep_version: "{{ toolchain_versions.AST_GREP_VERSION }}"
ast_grep_sha256: "{{ toolchain_versions.AST_GREP_SHA256_LINUX_AMD64 }}"
bazel_version: "{{ toolchain_versions.BAZEL_VERSION }}"
bazelisk_version: "{{ toolchain_versions.BAZELISK_VERSION }}"
bazelisk_sha256: "{{ toolchain_versions.BAZELISK_SHA256_LINUX_AMD64 }}"
oasdiff_version: "{{ toolchain_versions.OASDIFF_VERSION }}"
oasdiff_sha256: "{{ toolchain_versions.OASDIFF_SHA256_LINUX_AMD64_TARGZ }}"
oras_version: "{{ toolchain_versions.ORAS_VERSION }}"
oras_sha256: "{{ toolchain_versions.ORAS_SHA256_LINUX_AMD64 }}"
cosign_version: "{{ toolchain_versions.COSIGN_VERSION }}"
cosign_sha256: "{{ toolchain_versions.COSIGN_SHA256_LINUX_AMD64 }}"
oapi_codegen_version: "{{ toolchain_versions.OAPI_CODEGEN_VERSION }}"
nx_version: "{{ toolchain_versions.NX_VERSION }}"
pre_commit_version: "{{ toolchain_versions.PRE_COMMIT_VERSION }}"
molecule_version: "{{ toolchain_versions.MOLECULE_VERSION }}"
molecule_docker_version: "{{ toolchain_versions.MOLECULE_DOCKER_VERSION }}"
testinfra_version: "{{ toolchain_versions.TESTINFRA_VERSION }}"
ansible_runner_version: "{{ toolchain_versions.ANSIBLE_RUNNER_VERSION }}"
ansible_builder_version: "{{ toolchain_versions.ANSIBLE_BUILDER_VERSION }}"
ansible_navigator_version: "{{ toolchain_versions.ANSIBLE_NAVIGATOR_VERSION }}"
ggshield_version: "{{ toolchain_versions.GGSHIELD_VERSION }}"
checkov_version: "{{ toolchain_versions.CHECKOV_VERSION }}"
semgrep_version: "{{ toolchain_versions.SEMGREP_VERSION }}"
cve_bin_tool_version: "{{ toolchain_versions.CVE_BIN_TOOL_VERSION }}"
oxc_release_tag: "{{ toolchain_versions.OXC_RELEASE_TAG }}"
oxlint_version: "{{ toolchain_versions.OXLINT_VERSION }}"
oxlint_sha256: "{{ toolchain_versions.OXLINT_SHA256_LINUX_AMD64 }}"
oxfmt_version: "{{ toolchain_versions.OXFMT_VERSION }}"
oxfmt_sha256: "{{ toolchain_versions.OXFMT_SHA256_LINUX_AMD64 }}"
ruff_version: "{{ toolchain_versions.RUFF_VERSION }}"
ruff_sha256: "{{ toolchain_versions.RUFF_SHA256_LINUX_AMD64 }}"
gitleaks_version: "{{ toolchain_versions.GITLEAKS_VERSION }}"
gitleaks_sha256: "{{ toolchain_versions.GITLEAKS_SHA256_LINUX_AMD64 }}"
kubectl_version: "{{ toolchain_versions.KUBECTL_VERSION }}"
kubectl_sha256: "{{ toolchain_versions.KUBECTL_SHA256_LINUX_AMD64 }}"
helm_version: "{{ toolchain_versions.HELM_VERSION }}"
helm_sha256: "{{ toolchain_versions.HELM_SHA256_LINUX_AMD64 }}"
terraform_version: "{{ toolchain_versions.TERRAFORM_VERSION }}"
terraform_sha256: "{{ toolchain_versions.TERRAFORM_SHA256_LINUX_AMD64 }}"
kustomize_version: "{{ toolchain_versions.KUSTOMIZE_VERSION }}"
kustomize_sha256: "{{ toolchain_versions.KUSTOMIZE_SHA256_LINUX_AMD64 }}"
kind_version: "{{ toolchain_versions.KIND_VERSION }}"
kind_sha256: "{{ toolchain_versions.KIND_SHA256_LINUX_AMD64 }}"
ansible_lint_version: "{{ toolchain_versions.ANSIBLE_LINT_VERSION }}"
