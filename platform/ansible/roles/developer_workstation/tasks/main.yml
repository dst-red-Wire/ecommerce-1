---
- name: Reconcile workstation prerequisites
  ansible.builtin.import_tasks: prerequisites.yml

- name: Configure repository hooks after toolchain reconciliation
  ansible.builtin.import_tasks: hooks.yml
