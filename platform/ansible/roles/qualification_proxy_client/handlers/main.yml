---
- name: Reload systemd for the Docker proxy drop-in
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Restart Docker when an existing daemon proxy changes
  ansible.builtin.systemd_service:
    name: docker
    state: restarted
  when: "'docker.service' in ansible_facts.services"
