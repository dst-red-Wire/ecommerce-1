# Canonical project secret contract.
# Real values belong only in $HOME/.config/ecommerce-1/.env.
# Add future project secrets here as empty assignments only.

TERRAFORM_STATE_POSTGRESQL_PASSWORD=
TERRAFORM_STATE_BACKUP_AGE_IDENTITY=
TERRAFORM_STATE_BACKUP_AGE_RECIPIENT=
