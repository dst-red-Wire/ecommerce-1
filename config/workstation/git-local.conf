# Repository-owned Git defaults. Identity is intentionally not managed here.
core.autocrlf=input
core.eol=lf
fetch.prune=true
fetch.pruneTags=true
pull.ff=only
push.default=simple
rerere.enabled=true
rebase.autoStash=false
