# Historical local environment

The local Docker Desktop/WSL bootstrap, Gitea and `registry:2` remain historical
developer tooling. They are not referenced by integration, preproduction or
production and are not a deployment target for the remote staging workflows.
