# user-profile migrations

This directory is owned exclusively by the `user-profile` service. Database migrations will be added with that domain's implementation milestone; no other service may read its database directly.
