# search migrations

This directory is owned exclusively by the `search` service. Database migrations will be added with that domain's implementation milestone; no other service may read its database directly.
