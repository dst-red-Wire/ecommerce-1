---
profile: basic
skip_list:
  # Existing role variables predate role-prefix enforcement; renaming them is a
  # separate compatibility migration, not part of validation closure.
  - var-naming[no-role-prefix]
  # The management play intentionally fans independent hosts out with strategy free.
  - run-once[play]
