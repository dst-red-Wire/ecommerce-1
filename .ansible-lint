---
profile: basic
exclude_paths:
  # These are architecture/Kubernetes contracts, not Ansible task files.
  - architecture.lock.yaml
  - .pre-commit-config.yaml
  - config/
  - platform/gitops/
  - platform/tekton/
